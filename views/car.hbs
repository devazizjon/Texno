<div class="conteiner">
    <div class="row">
        <div class="col s12">
            <div class="car">
    <img src="{{car.img}}" alt="{{car.title}}" >
    <h3>{{car.title}}</h3>
    <p class="price">{{car.price}}</p>
</div>
        </div>
    </div>
</div>